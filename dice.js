let big = 6
let small = 1
let rand =  function (num1, num2) {
  console.log (Math.floor(Math.random() * (big - small + 6)) + 1)
  return (num1 + num2)
}
rand(4, 5);




length()
descriptopn: retruns the length of a string

example:
var omar = 'General Assembly';
console.log(omar.length);

replace()
description: replaces a value with another
example: 
str = "thank for the thing!";
var n = str.replace("thing", "lesson");

toUpperCase()
description: convert a string to upper case

example
var value1 = "Hello class!";       
var value2 = text1.toUpperCase();

